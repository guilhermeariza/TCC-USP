[package]
name = "structures-comparison"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# tempfile might be useful for LSM SSTables if we want atomic writes, but simple file io is fine.
